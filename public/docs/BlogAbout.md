# 關於我頁面 (BlogAbout.vue)

## 核心功能

展示個人簡介、技能分佈、聯絡表單及二維碼展示。

## 技術重點

- **多維資訊展示**：使用 `a-tabs` 分隔個人介紹、技能經歷與聯絡表單。
- **動態樣式**：整合 `useTheme` 響應深色模式，自動切換頭像背景。
- **組件應用**：利用 `a-qrcode` 生成動態連結，並透過 `a-drawer` 展示合作清單。