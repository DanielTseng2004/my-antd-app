# 部落格首頁 (BlogHome.vue)

## 核心功能

站點入口導覽，提供精選輪播、系統統計與快速操作面板。

## 技術重點

- **視覺呈現**：使用 `a-carousel` 製作自動播放的精選文章輪播。
- **即時回饋**：整合 `notification` 提供探索提示，並使用 `message` 反饋訂閱動作。
- **數據摘要**：透過 `a-statistic` 展示全站文章與分類統計數據。